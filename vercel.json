{
    "version": 2,
    "builds": [
        {
        "src": "backend/server.js",
        "use": "@vercel/node"
        }
    ],
    "rewrites": [
        {
        "source": "/api/(.*)",
        "destination": "backend/server.js"
        }
    ],
    "buildCommand": "npm run build --prefix frontend"
}